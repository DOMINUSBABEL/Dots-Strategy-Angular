import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.babylonia.dotsstrategy',
  appName: 'Dots Strategy',
  webDir: 'dist/dots-strategy-angular/browser'
};

export default config;
